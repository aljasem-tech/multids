[pytest]
pythonpath = src
markers =
    integration: marks tests as integration tests that require external services (use -m integration to run)
    real_integration: marks tests requiring user-provided cloud credentials
    benchmark: marks opt-in connector benchmark entry points
